import Foundation

# SwiftLint: the project includes a minimal configuration. Add or tune rules as desired.

disabled_rules:
  - trailing_whitespace
  - line_length

opt_in_rules:
  - empty_count
  - explicit_init
